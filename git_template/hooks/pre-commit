#!/bin/bash

# dir="$HOME"/some/directory
# if [ "$PWD" = "$dir" ]; then
#   [ \"$DISABLE_PRETTIER_HOOK\" == \"true\" ] || git rev-parse -q --verify MERGE_HEAD || yarn pretty-quick --staged
# fi

dotfiles_dir="$HOME"/dotfiles
if [ "$PWD" = "$dotfiles_dir" ]; then
  save_code_extensions
  git add code_extensions
fi
